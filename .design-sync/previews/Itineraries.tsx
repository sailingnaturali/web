import { Itineraries } from 'web'

// The full itineraries page: intro, tiered trip-card grid (featured trip spans
// both columns), route maps, CTA band, and footer. Takes no props — content
// comes from the page registry in src/lib/itineraries.ts.
export const FullPage = () => <Itineraries />
